package org.example;

public class QuickSort {
    //for Quick sort, we have  a pivot number thats the end of the arry for instance
    //we have two markers, Right and left, the left checks for values greater than the pivot,
    // while the right checks for values less than the pivot,
    //if they both meet the,during the loop(LR), the value swaps with the pivot .
    //note in point , if the left matcher run into the right it doesnt stop, it cont to move
    public  void quickSort(int[] arr){

    }
}
